<!DOCTYPE html>
<!-- paulirish.com/2008/conditional-stylesheets-vs-css-hacks-answer-neither/ -->
<!--[if lt IE 7 ]> <html class="ie6" lang="en"> <![endif]-->
<!--[if IE 7 ]>    <html class="ie7" lang="en"> <![endif]-->
<!--[if IE 8 ]>    <html class="ie8" lang="en"> <![endif]-->
<!--[if (gte IE 9)|!(IE)]><!--> <html lang="en"> <!--<![endif]-->
  <head>
    <title><%= page_title %></title>

    <!-- Set the viewport width to device width for mobile -->
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <%= stylesheet_link_tag :application, :media => 'all' %>
    <%= javascript_include_tag "vendor/custom.modernizr" %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <nav class="navbar navbar-default" role="navigation">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">Two Month Me!</a>
      <p class="navbar-text">Light some fire under some asses.</p>

      <% if @user %>
        <p class="navbar-text pull-right">Signed in as <a href="#" class="navbar-link"><%= @user.name %></a></p>
        <a href="/logout" class="tiny button secondary">Logout</a>
      <% else %>
        <a href="/login" class="btn btn-default navbar-btn">Sign in</a>
      <% end %>


    </div><!-- /.navbar-collapse -->
    </nav>
    <%= render 'flashes' -%>

    <%= yield %>

    <%= render 'javascript' %>
  </body>
</html>
